import localFont from "next/font/local";

/** Bride & groom names only — uses public/GreatVibes-Regular.ttf */
export const greatVibesRegular = localFont({
  src: "../../public/GreatVibes-Regular.ttf",
  display: "swap",
  weight: "400",
});
